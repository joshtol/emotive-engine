/**
 * AudioDeformer Stub — Minimal Build
 * Pass-through implementation. Returns points unchanged (no audio deformation).
 */
export class AudioDeformer {
    applyAudioDeformation(points) { return points; }
    setAudioDeformation() {}
    setVocalEnergy() {}
}
