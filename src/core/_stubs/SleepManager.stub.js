/**
 * SleepManager Stub — Minimal Build
 * EmotiveRenderer creates new SleepManager(this).
 * Calls enterSleepMode(), wakeUp(), renderSleepIndicator().
 */
export class SleepManager {
    enterSleepMode() {}
    wakeUp() {}
    renderSleepIndicator() {}
    cleanup() {}
}
